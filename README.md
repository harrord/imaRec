# 人生记录器 (imaRec)

**人生记录器 (imaRec)** 是一款为腾讯ima开发的安卓应用。它的核心工作流是：在本地进行录音，随后自动上传至ima进行处理与存档。

本 APP 可作为最近几年爆火的 AI 硬件产品——录音卡片这个品类的平替。不仅对于那些喜欢手机套壳的人友好，而且录音卡片当中会议总结、纪要、翻译、区分说话人、脑图等等功能也都可以实现，并且完全免费。单次录音上限两小时，总量则没有上限。

在 AI 时代，个人数据变得前所未有地重要。时间会流逝，记忆会变形，而记录是为了让记忆转化为数据，永久地留存下来。我们致力于将所有的个人数据留存，以实现某种意义上的“赛博永生”。

本应用的最初设计目标是 **24 小时全天候录取你身边值得被记录下的声音**，尽可能为你留下身边那些值得被记录的声音，而无关的噪音则不予记录。

请移步 release 页面下载 apk 后安装。

## ✨ 核心理念与智能规则

为了实现全天候、智能化的声音记录，应用内置了多种中断与重启规则：
- **定时中断**：例如在晚上睡觉时，可以选择设定定时自动中断录音。
- **环境自适应**：当外部声音分贝过小（说明正处于极其安静的环境）时，应用会自动中断录音以节省资源。
- **智能唤醒**：当你重新开始步行、或者到达某个特定地点后，录音将会重新启动。

**🔋 耗电表现**
我们对全天候录音进行了耗电测试：连续录音 **1.5 小时约仅消耗 1% 的电量**。在保证功能完整的同时，耗电量完全在可接受的范围内。

## ✨ 第一版功能特性

目前第一版已实现以下核心功能：
- **本地与云端联动**：录制本地声音并准备上传至腾讯艾玛 (ima) 服务器。
- **录音管理**：主页面直观展示所有录音文件列表，方便您浏览和管理。
- **一键录音**：采用底部中央大型按钮设计，支持一键快速开始或结束录音。
- **高品质存储**：录音文件默认保存为高质量的 `.m4a` 格式，兼顾音质和存储空间。
- **后台稳定录音**：录音期间会显示前台服务通知及锁屏通知，确保应用退至后台时录音不被中断。
- **通知栏便捷控制**：支持直接在通知栏内暂停或继续录音，操作更加灵活。
- **设置入口**：主界面右上角预留了设置页面入口（功能持续完善中）。

## 🔒 锁屏卡片及通知栏快捷操作

本应用提供了强大的前台通知及锁屏控制卡片，无需解锁屏幕，即可直接在锁屏界面或下拉通知栏对录音进行精细化管理。针对锁屏组件的各按钮，设计了**连续点击**的快捷操作：

### 1. ⏸️ 暂停/继续按钮 (Toggle Pause)
*   **单次/首次点击**：进入“暂停选择窗口”（开启 5 秒倒计时，倒计时期间录音继续进行）。通知栏和锁屏卡片反馈行会显示“5 秒后暂停”提示，暂停按钮文案显示“暂停”。
*   **5 秒内连续点击**：循环切换不同的暂停模式，每次点击都会重新开始 5 秒倒计时。支持 4 档循环：
    *   **第 1 次点击（或 4n+1 次）**：一直暂停（倒计时结束后需要手动恢复）。
    *   **第 2 次点击（或 4n+2 次）**：定时暂停 X 分钟（例如 5 分钟，具体由设置中的 `pauseMinutesX` 决定）。
    *   **第 3 次点击（或 4n+3 次）**：定时暂停 Y 分钟（例如 10 分钟，由 `pauseMinutesY` 决定）。
    *   **第 4 次点击（或 4n 次）**：定时暂停 Z 分钟（例如 30 分钟，由 `pauseMinutesZ` 决定）。
*   **5 秒内无再次点击**：倒计时结束后真正执行暂停，并根据选择的模式：
    *   若为“一直暂停”：需手动再次点击“继续”恢复录音。
    *   若为“定时暂停”：通知栏和锁屏卡片将显示剩余时间倒计时（如“暂停剩余 N 分钟”），倒计时归零时自动恢复录音。
*   **暂停状态下点击**：直接恢复录音，返回录音状态。

### 2. 📂 分组按钮 (Switch Knowledge Base)
*   **启用条件**：主界面已展开的知识库 Tab 数量 $\ge 2$，且当前处于录音状态下。
*   **单次/首次点击**：在已展开的知识库列表中循环切换到下一个知识库，并弹出 Toast 提示（如“连续点击以切换保存的知识库。当前选择的「xxx」，5 秒后自动执行分段”），同时在通知卡片的反馈行中显示“当前选中：xxx，5 秒后自动分段”的提示（锁屏可见）。同时启动 5 秒后台倒计时。
*   **5 秒内连续点击**：取消上一次倒计时，循环切换到下一个知识库，并重新启动 5 秒倒计时。
*   **5 秒内无再次点击**：倒计时结束后，清除反馈提示，并**自动执行分段**（将当前片段文件名标签重写为切换后的新知识库并触发上传，同时立即开启新片段继续录音）。

### 3. ✂️ 分段按钮 (Manual Segment)
*   **单次点击**：在录音状态下，点击可立即结束当前录音片段并保存/上传（保持原本的知识库归属），同时立即开启一个新片段继续录音。
*   **反馈提示**：分段完成后，将在通知卡片反馈行提示“录音片段已保存到「xxx」”，5 秒后该提示自动消失。

## ⚙️ 联动ima配置

**人生记录器** 需要和腾讯的ima联动配合使用。ima提供了移动端、桌面端以及网页端等多种使用方式。

为了让本 APP 能够自动将录音上传至服务器，请按照以下步骤获取和使用相关配置：
1. 访问ima开发者接口页面：[https://ima.qq.com/agent-interface](https://ima.qq.com/agent-interface)
2. 用您自己的账户登录该网页。
3. 在页面中获取您的 **API key**和**Client ID**。
4. 将获取到的 API key 配置到“人生记录器” APP 中（在设置页面），即可实现数据自动上传与联动。

## 🚀 如何运行和使用

### 1. 开发环境要求
### 2. 获取代码
### 3. 构建与安装
 以上 3 个步骤，请完整地下载本项目的源码并询问 AI。本文档在此不赘述。
 
### 4. 权限说明
为了让全天候后台录音应用正常运行，首次打开及使用时会请求必要的权限：
- **麦克风权限 (`RECORD_AUDIO`)**：必须的权限，用于采集环境声音。
- **通知权限 (`POST_NOTIFICATIONS`)**（对于 Android 13 及以上）：用于显示后台录音的控制通知栏。
- **忽略电池优化 / 后台耗电权限**：**（极其重要）** 为了防止系统杀掉后台进程，确保 24 小时记录不被中断，您必须在系统设置中允许应用后台耗电或将其加入无限制白名单。

## 🤝 参与贡献

欢迎大家提出 Issue 或提交 Pull Request 来帮助改进这个项目！

## 📜 开源协议

本项目基于 **GPLv3 (GNU General Public License v3.0)** 协议开源。

这意味着您可以自由地使用、修改和分发本项目的代码，但如果您基于本项目进行了修改并分发，您的衍生作品也必须遵守 GPLv3 协议开源。详情请查阅项目根目录下的 [LICENSE](LICENSE) 文件。
